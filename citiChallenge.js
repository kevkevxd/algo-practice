// function solution(a) {
//   // find the longest chain of consecutive nums
//   for (let i = 0; i < a.length; i++) {}
//   // flip the ones before/after the chain + count it.
// }

function coins(a) {
  let newArr = a.slice(1);
  if (a[0] === 1) {
  }
}
